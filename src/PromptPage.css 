/* General Body Styles (ensure consistency with Home.css or define once globally if preferred) */
body {
  margin: 0;
  font-family: 'Open Sans', sans-serif;
  background-color: #f4f7f6;
  color: #333;
  line-height: 1.6;
}

.prompt-page-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem;
  min-height: 100vh;
  box-sizing: border-box;
}

/* Header */
.prompt-page-header {
  width: 100%;
  max-width: 900px;
  display: flex;
  align-items: center;
  justify-content: space-between; /* Pushes button to left, title to center */
  margin-bottom: 2rem;
  position: relative; /* For absolute positioning of back button on smaller screens */
}

.prompt-page-header h1 {
  font-family: 'Poppins', sans-serif;
  font-size: 2.5rem;
  color: #2c3e50;
  flex-grow: 1; /* Allows heading to take up available space */
  text-align: center;
  margin: 0;
  padding: 0 50px; /* Add padding to prevent text overlapping button */
}

.back-button {
  background-color: #6a11cb; /* Purple back button */
  color: white;
  border: none;
  border-radius: 50px;
  padding: 10px 20px;
  font-size: 0.9rem;
  cursor: pointer;
  transition: background-color 0.3s ease, transform 0.2s ease;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  z-index: 10; /* Ensure button is above other elements if positioned absolutely */
}

.back-button:hover {
  background-color: #550aab;
  transform: translateY(-1px);
}

/* Model Info Section */
.model-info-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 2rem;
  background-color: #ffffff;
  padding: 1.5rem;
  border-radius: 12px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
  width: 100%;
  max-width: 700px;
  text-align: center;
}

.model-avatar {
  width: 80px;
  height: 80px;
  object-fit: contain;
  margin-bottom: 1rem;
  border-radius: 50%; /* Make it circular if images allow */
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.model-info-section p {
  font-size: 1.1rem;
  color: #555;
}

/* Prompt Area */
.prompt-area-wrapper {
  background-color: #ffffff;
  border-radius: 12px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
  padding: 2rem;
  width: 100%;
  max-width: 900px;
  margin-bottom: 2rem;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.prompt-textarea {
  width: 100%;
  padding: 15px;
  font-size: 1rem;
  border: 1px solid #ddd;
  border-radius: 8px;
  margin-bottom: 1.5rem;
  resize: vertical; /* Allow vertical resizing */
  min-height: 120px;
  box-sizing: border-box; /* Include padding and border in the element's total width and height */
  font-family: 'Open Sans', sans-serif;
}

.prompt-textarea:focus {
  outline: none;
  border-color: #2575fc; /* Blue border on focus */
  box-shadow: 0 0 0 3px rgba(37, 117, 252, 0.2); /* Soft blue glow on focus */
}

.submit-button {
  background-color: #2575fc;
  color: white;
  border: none;
  border-radius: 50px;
  padding: 15px 30px;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  transition: background-color 0.3s ease, transform 0.2s ease;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.submit-button:hover:not(:disabled) {
  background-color: #1a5acb;
  transform: translateY(-2px);
}

.submit-button:disabled {
  background-color: #ccc;
  cursor: not-allowed;
  box-shadow: none;
}

.error-message {
  color: #e74c3c; /* Red error message */
  margin-top: 1rem;
  font-weight: bold;
}

/* Response Area */
.response-area {
  background-color: #e8f5e9; /* Light green background for response */
  border: 1px solid #c8e6c9;
  border-radius: 12px;
  padding: 2rem;
  width: 100%;
  max-width: 900px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
  margin-bottom: 2rem;
}

.response-area h3 {
  font-family: 'Poppins', sans-serif;
  font-size: 1.8rem;
  color: #2c3e50;
  margin-bottom: 1rem;
  text-align: center;
}

.model-response-text {
  white-space: pre-wrap; /* Preserves whitespace and wraps text */
  word-wrap: break-word; /* Breaks long words */
  font-family: 'Open Sans', sans-serif;
  font-size: 1rem;
  color: #444;
  background-color: #f8fcf9; /* Very light background for the text block */
  padding: 1rem;
  border-radius: 8px;
  border: 1px dashed #dcdcdc; /* Dashed border for response */
  overflow-x: auto; /* Adds horizontal scrollbar if content is too wide */
}

/* Tips Section */
.tips-section {
  background-color: #f0f8ff; /* Light blue background */
  border: 1px solid #d4edda;
  border-radius: 12px;
  padding: 1.5rem 2rem;
  width: 100%;
  max-width: 900px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
  margin-bottom: 2rem;
}

.tips-section h3 {
  font-family: 'Poppins', sans-serif;
  font-size: 1.5rem;
  color: #2c3e50;
  margin-bottom: 1rem;
  text-align: center;
}

.tips-section ul {
  list-style-type: disc;
  padding-left: 20px;
  color: #555;
}

.tips-section li {
  margin-bottom: 0.5rem;
  font-size: 0.95rem;
}

/* Footer */
.prompt-page-footer {
  width: 100%;
  padding: 1rem 0;
  background-color: #333;
  color: #f4f7f6;
  text-align: center;
  font-size: 0.85rem;
  margin-top: auto; /* Pushes the footer to the bottom of the flex container */
}

/* Responsive Adjustments */
@media (max-width: 768px) {
  .prompt-page-header {
    flex-direction: column;
    text-align: center;
    gap: 1rem;
    padding-top: 1rem; /* Add some space at top */
  }

  .prompt-page-header h1 {
    font-size: 2rem;
    padding: 0; /* Remove horizontal padding on small screens */
  }

  .back-button {
    align-self: flex-start; /* Keep back button to the left */
    margin-bottom: 1rem; /* Space between button and title */
  }

  .prompt-page-container {
    padding: 1rem;
  }

  .prompt-textarea {
    min-height: 100px;
  }

  .submit-button {
    padding: 12px 25px;
    font-size: 1rem;
  }

  .response-area,
  .tips-section,
  .model-info-section,
  .prompt-area-wrapper {
    padding: 1.5rem;
  }
}

@media (max-width: 480px) {
  .prompt-page-header h1 {
    font-size: 1.8rem;
  }

  .prompt-textarea {
    min-height: 80px;
  }

  .response-area h3 {
    font-size: 1.5rem;
  }
}