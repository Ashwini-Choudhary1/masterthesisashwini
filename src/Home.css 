/* General Body Styles - important for consistency across pages */
body {
  margin: 0;
  font-family: 'Open Sans', sans-serif; /* Fallback to sans-serif */
  background-color: #f4f7f6; /* Light background for entire app */
  color: #333; /* Dark gray for text */
  line-height: 1.6;
}

/* Home Container */
.home-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100vh; /* Ensures footer stays at bottom */
  box-sizing: border-box;
}

/* Hero Section */
.hero-section {
  background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%); /* Purple to blue gradient */
  color: white;
  padding: 4rem 2rem;
  width: 100%;
  display: flex;
  flex-wrap: wrap; /* Allows content to wrap on smaller screens */
  justify-content: center;
  align-items: center;
  text-align: center;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
  gap: 2rem; /* Spacing between content and image */
}

.hero-content {
  max-width: 600px;
}

.hero-content h1 {
  font-family: 'Poppins', sans-serif;
  font-size: 3.5rem;
  margin-bottom: 1rem;
  line-height: 1.2;
}

.hero-content p {
  font-size: 1.2rem;
  margin-bottom: 1.5rem;
  opacity: 0.9; /* Slightly faded text */
}

.hero-image img {
  max-width: 300px; /* Adjust as needed */
  height: auto;
  border-radius: 8px; /* Slightly rounded corners */
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.2); /* Shadow for the image */
}

/* Model Selection Section */
.model-selection-section {
  padding: 3rem 2rem;
  width: 100%;
  max-width: 1200px; /* Constrain width for larger screens */
  text-align: center;
}

.model-selection-section h2 {
  font-family: 'Poppins', sans-serif;
  font-size: 2.5rem;
  color: #2c3e50; /* Darker gray for headings */
  margin-bottom: 3rem;
}

.model-cards-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 2.5rem; /* Spacing between cards */
}

.model-card {
  background-color: #ffffff;
  border-radius: 12px;
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.08); /* More prominent shadow for cards */
  padding: 2.5rem;
  text-align: center;
  width: 380px; /* Fixed width for cards */
  transition: transform 0.3s ease, box-shadow 0.3s ease; /* Smooth hover effects */
  cursor: pointer;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.model-card:hover {
  transform: translateY(-8px); /* Lift effect on hover */
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15); /* Stronger shadow on hover */
}

.model-icon {
  width: 100px;
  height: 100px;
  margin-bottom: 1.5rem;
  object-fit: contain; /* Ensures the image fits without distortion */
}

.model-card h3 {
  font-family: 'Poppins', sans-serif;
  font-size: 1.8rem;
  color: #4a4a4a;
  margin-bottom: 1rem;
}

.model-card p {
  font-size: 1rem;
  color: #666;
  margin-bottom: 2rem;
  flex-grow: 1; /* Allows paragraph to take up available space, pushing button down */
}

.explore-button {
  background-color: #2575fc; /* Blue button */
  color: white;
  border: none;
  border-radius: 50px; /* Pill-shaped button */
  padding: 12px 25px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: background-color 0.3s ease, transform 0.2s ease;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.explore-button:hover {
  background-color: #1a5acb; /* Darker blue on hover */
  transform: translateY(-2px); /* Slight lift on hover */
}

/* Home Footer */
.home-footer {
  width: 100%;
  padding: 1.5rem 0;
  background-color: #333; /* Dark background */
  color: #f4f7f6; /* Light text */
  text-align: center;
  font-size: 0.9rem;
  margin-top: auto; /* Pushes the footer to the bottom of the flex container */
}

/* Responsive Adjustments */
@media (max-width: 768px) {
  .hero-section {
    padding: 3rem 1.5rem;
    flex-direction: column; /* Stack content and image vertically */
  }

  .hero-content h1 {
    font-size: 2.8rem;
  }

  .hero-content p {
    font-size: 1rem;
  }

  .hero-image img {
    max-width: 250px;
  }

  .model-selection-section {
    padding: 2rem 1rem;
  }

  .model-selection-section h2 {
    font-size: 2rem;
    margin-bottom: 2rem;
  }

  .model-card {
    width: 90%; /* Make cards wider on small screens */
    max-width: 350px; /* Ensure they don't get too wide */
    padding: 2rem;
  }

  .model-cards-container {
    gap: 1.5rem;
  }
}

@media (max-width: 480px) {
  .hero-content h1 {
    font-size: 2.2rem;
  }

  .model-card {
    width: 100%; /* Full width on very small screens */
    padding: 1.5rem;
  }

  .explore-button {
    padding: 10px 20px;
    font-size: 0.9rem;
  }
}